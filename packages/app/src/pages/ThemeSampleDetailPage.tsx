import { ThemeDetailsPage } from "@nl-portal/nl-portal-user-interface";

const ThemeSampleDetailPage = () => {
  const type = "sample";

  return (
    <ThemeDetailsPage type={type} titleTranslationId="Contract ...">
      *** Children ***
    </ThemeDetailsPage>
  );
};

export default ThemeSampleDetailPage;
